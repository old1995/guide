import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AddedRankPage } from './added-rank';

@NgModule({
  declarations: [
    AddedRankPage,
  ],
  imports: [
    IonicPageModule.forChild(AddedRankPage),
  ],
})
export class AddedRankPageModule {}
